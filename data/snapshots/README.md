# Historical Snapshots

Dynamic GitHub observations live here instead of overwriting history.

Recommended layout:

```text
data/snapshots/
└── YYYY/
    └── MM/
        └── YYYY-MM-DD.json
```

A snapshot may contain Stars, forks, Trending observations, releases and other dated signals. Every quantitative observation should carry a timestamp or observation date and should come from a verifiable source.
